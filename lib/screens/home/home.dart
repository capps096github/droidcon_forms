import '../../droidcon_exporter.dart';
import '../user/user_profile.dart';

class DroidconHome extends StatelessWidget {
  const DroidconHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const UserProfile();
  }
}
